include <stdio.h>
main(){
  int a,b,c,d,m,x1,x2,y1,y2,w,x,y,z,i,j,k;
  a = b * c;
  d = (b * b) - (4*(a * c));
  m = (y2 - y1) / (x2 - x1);
  i = x + y -z; 
  j = (x + y) / (z + w); 
  k = 5 / (1 + (x * x));
}