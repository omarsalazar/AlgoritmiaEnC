#include <stdio.h>
main(){
  int a,b,c,d;
  printf("\nIngresa 4 numeros separados por comas y los imprimire en el orden contrario del que los pusiste uwu. ");
  scanf("%d, %d, %d, %d", &a, &b, &c, &d);
  printf("\n%d, %d, %d, %d", &d, &c, &b, &a);  
}